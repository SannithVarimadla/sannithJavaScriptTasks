JavaScript Basics — 50 Questions
Variables
What is a variable in JavaScript?
-->variables are the names given to memory to store some values ,and which holds some data with a specific name.

What are the three keywords used to create variables?
--> var ,let and const

Write the syntax to create a variable using var.
--> var name="sannith";

Write the syntax to create a variable using let.
-->let age=24;

Write the syntax to create a variable using const.
-->const marks=100;

What is declaration?
-->creating a variable without assigning the values. eg. let a;

What is initialization?
-->assigning a value for a variable is called initialization.eg. a=2;

What is reassignment?
-->Assigning the values again to a existed variable.eg. let a=10;
                                                            a=20;
What is redeclaration?
-->creating a variable of same type with same datatype with a different value.eg.let age=22;
                                                                                 let age=24;
Which keyword allows redeclaration?
-->var keyword.

Which keyword allows reassignment?
-->var and let.

Which keyword requires initialization when declared?
-->const.

Identify the declaration and initialization:
let age = 25;
--> let age  --is the declaration.
-->age=25;   --is the initialization.

What is the value of a?
var a = 100;
console.log(a);
--> value of a is 100.

Change the value of this variable to 200:
var number = 100;
-->   number=200;

What will be the output?
var a = 10;
console.log(a);
--> on teh console it shows 10.

What will be the output?
var a = 10;
a = 20;
console.log(a);
-->its 20.

What will be the output?
var a = 10;
var a = 30;
console.log(a);
--> its 30.

Write a var variable named name with the value "John".
-->var name="John";

Create a var variable named price with the value 500.
Reassign price from 500 to 1000.
--> var price=500;
        price=1000;
    console.log(price);

What will be the output?
var x = 50;
x = 100;
console.log(x);
--> its 100.

Can a var variable be reassigned?
-->yes.

Can a var variable be redeclared?
-->yes.

Write an example of var redeclaration.
--> var amount=10;
    var amount=30;

Create a let variable named age with the value 25.
--> let age=25;

What will be the output?
let age = 20;
age = 30;
console.log(age);
--> its 30.

Can a let variable be reassigned?
-->yes.

Can a let variable be redeclared?
--> NO.

Find the error:
let name = "John";
let name = "David";

--> let variable cannot be re-declared.

Create a let variable called city and assign "Chennai".
Change the value of city to "Salem".
--> let city="chennai";
        city="Salam";

What will be the output?
let x = 10;
x = 50;
console.log(x);
--> its 50.

Write a let variable called salary with the value 25000.
Reassign salary to 30000.
-->let salary=25000;
        salary=30000;

Create a const variable called pi with the value 3.14.
--> const pi=3.14;

Can a const variable be reassigned?
--> No.

Can a const variable be redeclared?
--> No.

What is wrong with this code?
const age;
age = 25;
--> const variable needs to be initialized  while declaring.

What happens here?
const price = 500;
price = 1000;
--> it cause error,const variable cannot be reassigned.

Create a const variable called country with the value "India".
--> const country="India";

What will be the output?
const x = 100;
console.log(x);
--> its 100.

Which keyword should you use if the value should not be reassigned?
--> const
What is the difference between let and const?
--> let variable can be reInitialized but not reDeclared.
    const variable cannot be reInitialized and reDeclared and it need to initialized while declaring.

What is the difference between var and const?
-->var variable can be reInitialized and reDeclared,not need to initialized while declaring.
const variable cannot be reInitialized and reDeclared and it need to initialized while declaring.

Printing & Console
Write JavaScript code to print Hello World using console.log().
--> let name="Hello world";
    console.log(name);

Write JavaScript code to print the number 500 using console.log().
-->console.log(500);

What is the purpose of console.warn()?
--> to print a warning message on console,used for error handling also.

What is the purpose of console.error()?
--> to print a error message like  name is mandatery.

What is the purpose of each?
alert()--->It gives a popup on the browser like alert,it consits some message and we need to check it and its consist ok button.
prompt()-->used to get data or input from the user.
confirm()---> it checks the condition if yes then it allows to browser,it like a popup consist of ok and cancel button.
document.writeln()-->used to print data on the browser.
console.log()-->used to print data on console.